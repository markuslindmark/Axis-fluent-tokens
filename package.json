{
    "name": "fluentui-theme-generator",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "transform": "token-transformer tokens.json generated/tokens/global.json global && token-transformer tokens.json generated/tokens/light.json light && token-transformer tokens.json generated/tokens/dark.json dark",
        "build": "node --require ts-node/register build.ts"
    },
    "dependencies": {
        "token-transformer": "^0.0.19",
        "style-dictionary": "^3.1.1",
        "ts-node": "^10.5.0"
    }
}
